package com.example.java8features;
@FunctionalInterface
public interface MyFunctionalInterface {
	
	void getFunction();   // if only one abstract method we called as functional interface
	
	// we have three types of interface 
	//there are 1. normal interface 2. single abstract methods we called as functional interface
	
}

